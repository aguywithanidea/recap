= render '/members/partials/notice'

%h1 Members

%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Email
      %th{colspan:3}

  %tbody
    - @members.each do |member|
      %tr
        %td= member.name
        %td= member.email
        %td= link_to( 'Edit', edit_member_path(member), {class:'btn btn-info btn-sm'})
        -# %td= link_to 'Destroy', member, method: :delete, data: { confirm: 'Are you sure?' }
        -#%td= link_to( 'New Note', edit_member_path(member))


=link_to( 'New Member', new_member_path, {class:'btn btn-primary btn-lg', role:'button'})
