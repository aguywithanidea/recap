- errors ||= []
- if errors.count > 0
  #error_explanation
    %h2
      #{pluralize(errors.count, 'error')} prevents this from being saved:

    %ul
    - errors.full_messages.each do |err|
      %li= err
