- note ||= nil
- if note.present?
  %tr
    %td= note.member.name
    %td= labelize(note.tag.name)
    %td= note.note
    -#%td= link_to( 'Show', note)
    %td= link_to( 'Edit', edit_note_path(note))
    -# %td= link_to 'Destroy', note, method: :delete, data: { confirm: 'Are you sure?' }
  
  